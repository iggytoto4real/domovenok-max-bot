# backend-core URL для сборки / когда нужен прямой доступ к порту 8080:
# VITE_API_URL=http://localhost:8080
#
# Локально с npm run dev: НЕ задавай VITE_API_URL (или оставь пустым) —
# запросы пойдут на тот же хост (/api), Vite проксирует на localhost:8080.
